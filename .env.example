# 复制为 .env 后按需填写。不填 PUBLIC_SUPABASE_* 时：开发环境自动走本地 mock 身份，生产环境登录入口关闭，公开阅读不受影响。

# Supabase 项目设置 → API 里的两个值。anon key 设计上就是公开的，安全边界在数据库 RLS，不要填 service_role key。
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=

# 站点绝对地址，同时用于 OAuth 回调与 CSRF 同源判定。自托管（node 适配器）时必须是访问者实际使用的地址，端口也要带上。
SITE_URL=http://localhost:4321

# 部署在子路径时填写，例如 /cardistry-museum/；根路径留空或写 /
BASE_PATH=/

# node（自托管，默认）或 cloudflare
DEPLOY_TARGET=node

# 本地 mock 身份服务所用 cookie 的签名密钥，仅开发环境需要，生产环境留空。
DEV_AUTH_SECRET=

# 本地 mock 的站点开关：置 true 时相当于全局开放编辑，用于验证贡献者提交路径。
MOCK_OPEN_EDITING=
